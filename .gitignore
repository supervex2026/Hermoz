# Node
node_modules/
dist/
dist-ssr/
*.local
npm-debug.log*

# Rust / Tauri
src-tauri/target/
src-tauri/gen/

# Env / secrets - never commit real keys
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Local database (Phase 4)
database/*.sqlite
database/*.db

# Release binaries — ship via GitHub Releases, not git
releases/
*.exe

# Duplicate media assets at repo root (canonical copies live in public/)
momo logo.png
Intro_animation__*.mp4
intro animation.zip
